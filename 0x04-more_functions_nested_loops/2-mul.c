#include "main.h"
/**
 * mul - Multiplies two integers
 * @x: The first number to be multiply
 * @y: The second number to multiply with first number
 *
 * Return: The result of multiplication between numbers
 */

int mul(int x, int y)
{
	return (x * y);
}
